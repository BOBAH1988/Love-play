# Зависимости для локальных проверок (не часть приложения)
node_modules/

# Служебные файлы ИИ-ассистентов (остаются локально, в репозиторий не идут)
.kilo/
.clineignore
.cursor/
.windsurf/
.aider*
AGENTS.local.md

# Секреты и ключи — НИКОГДА не коммитить
*.key
*.pem
secrets.json
*-secrets.json
# Токен Яндекс Диска: лежит в отдельном файле рядом с index.html и НЕ попадает
# в репозиторий. Приложение читает его оттуда (см. games/fants-davay.js).
yandex-token.js
config.local.js

# Системное
.DS_Store
Thumbs.db

# Временные файлы и посторонние заметки (не часть игры)
.tmp-*
*.log
DSH-*.md
*.local.md
