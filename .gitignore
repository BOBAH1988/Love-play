# Зависимости для локальных проверок (не часть приложения)
node_modules/

# Служебные файлы ИИ-ассистентов (остаются локально, в репозиторий не идут)
.kilo/
.clineignore
.cursor/
.windsurf/
.aider*
AGENTS.local.md

# Секреты и ключи — НИКОГДА не коммитить
*.key
*.pem
secrets.json
*-secrets.json
# Секретов в проекте нет и быть не должно: приложение статическое, любой ключ
# в коде виден посетителю (см. раздел «Секреты» в tools/check.js).
config.local.js

# Системное
.DS_Store
Thumbs.db

# Временные файлы и посторонние заметки (не часть игры)
.tmp-*
*.log
DSH-*.md
*.local.md
